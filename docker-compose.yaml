version: '3.8'
services:
  moneybasketball-api:
    build: 
      context: .
      dockerfile: Dockerfile.backend
    ports: ["5000:5000"]
  moneybasketball-fe:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "80:80"